# コスト試算 - Bike Specs Lookup App

## 前提条件

- **リージョン**: ap-northeast-1（東京）
- **為替**: 1 USD = 150円で換算
- **稼働時間**: 24時間 × 30日 = 720時間/月（常時稼働想定）
- **利用想定**: 学習目的、自分1人、1日数回アクセス
- **算出時点**: 2026年5月（料金は変動するため要確認）
- **AWS無料クレジット**: 約$159（2026年8月7日失効）

## 月額コスト試算（通常稼働時）

### 常時課金リソース

| サービス | 仕様 | 月額（円） | 備考 |
|---|---|---|---|
| ALB | 1台、LCU平均0.5 | 約2,500 | 時間単価 + LCU課金 |
| ECS Fargate | 0.25 vCPU / 0.5 GB × 1タスク × 720h | 約1,400 | Auto Scaling停止時最小1タスク |
| Aurora Serverless v2 | 平均 0.2 ACU × 720h | 約1,000 | 使用時のみ課金、Auto Pause効く |
| Aurora ストレージ | 1 GB | 約20 | 使用量課金、学習用途では微々たる |
| VPC Endpoint（Interface）× 4 | 各720h + データ処理料 | 約4,200 | Interface型は1個あたり月1,000円強 |
| Secrets Manager | シークレット1個 + API 1,000回以下 | 約60 | |
| CloudWatch Logs | 1 GB取り込み、保管30日 | 約100 | |
| CloudTrail + S3 | 管理イベント記録、90日保持 | 約100 | 管理イベントは最初のトレイルが無料、S3ストレージのみ課金 |
| ECR | 500 MB保管 | 約15 | |
| **合計** | | **約9,400** | Route 53不使用（Cloudflare DNS利用） |

> **Route 53 不使用**: DNS は Cloudflare（無料プラン）を利用。Route 53 のホストゾーン費用（月$0.5）・クエリ費用は発生しない。

### NAT Gateway採用時との比較（参考）

| 構成 | 月額（円） | 差分 |
|---|---|---|
| **VPC Endpoint 4個構成（今回採用）** | 約9,400 | - |
| NAT Gateway構成 | 約10,400 | +1,000 |

## 重要な注意点

VPC EndpointはNAT Gatewayの代替として「コスト削減」と言われがちだが、
**Interface型VPC Endpointは1個あたり月約1,000円+データ処理料**がかかる。
4個使うと約4,200円に達し、NAT Gateway（月5,000円）との差が小さい。

### コスト最適化の選択肢

#### 選択肢1: VPC Endpointを減らす
必須なのは以下のみ:
- `ecr.dkr`, `ecr.api`（Interface）: コンテナPull必須
- `s3`（Gateway、**無料**）: ECRイメージレイヤー格納先
- `secretsmanager`（Interface）: DB認証情報取得必須
- `logs`（Interface）: CloudWatchログ出力

→ Interface型4個のまま、これ以上減らすと機能が壊れる。

#### 選択肢2: 使わない時間帯は停止する（最有力）

- ECS Serviceのdesired count = 0 にするスクリプト
- **実質コストを月3,000円程度まで圧縮可能**

### 推奨運用: 「使う時だけON」スクリプト戦略

平日 9:00-22:00 だけ起動した場合の月額試算:
- 稼働時間: 13h × 22日 = 286時間（通常720時間の約40%）
- ALB: 2,500 × 0.4 = 約1,000円
- ECS: 1,400 × 0.4 = 約560円
- VPC Endpoint: 4,200 × 0.4 = 約1,680円
- Aurora: 変わらず 約1,000円（ACU 0で待機）
- その他固定費: 約400円
- **合計: 約4,640円**

## 無料クレジットの活用

- 現在の残クレジット: 約$159（約¥24,000）
- 失効日: **2026年8月7日**
- 月額$63（約9,400円）で運用してもクレジット内で約2.5ヶ月分カバー可能

### 失効前の対応チェックリスト

- [ ] 2026年7月24日: リソース棚卸し（2週間前）
- [ ] 2026年8月1日: 全リソース停止（1週間前）
- [ ] 2026年8月7日: 最終確認（失効当日）

**AWSのBillingアラートはクレジット失効「後」の課金のみ検知する**。
クレジット失効前にはアラームは発動しないため、カレンダー管理が必須。

## 負荷試験時の追加コスト（実績）

Phase 3-A で実施した負荷試験の一時的コスト増:
- ECSタスク数が1→2にスケールアウト（CPUストレス試験で発火確認済み）
- Aurora ACUが0.2→1.0に急増 → 数時間で約50円
- ALB LCUが増加 → 数時間で約30円

**負荷試験1回あたり: 約100〜200円追加**。許容範囲。

## コストアラート設定状況

CloudWatch Billing アラームで以下を設定済み（us-east-1）:
- `billing-warning-20usd`: 月額$20（約3,000円）到達時にメール通知
- `billing-critical-53usd`: 月額$53（約8,000円）到達時に緊急通知

**注意**: 無料クレジット消費中はアラートが発動しない。
クレジット失効後、または予想外の課金時にのみ機能する設計。

## 学習完了後のコストゼロ化手順

フェーズ完了後、継続コストを発生させないための完全停止手順:

1. `terraform destroy` で全リソース削除
2. ECRイメージを保持する場合は手動で削除（月数円）

**残してもいいもの**:
- ECRイメージ（月数円）
- Cloudflareのドキュメント（無料）
- GitHubリポジトリ（無料）

→ 停止後の月額: **ほぼ0円**
